services:
  - type: web
    name: cpq-proposal-generator
    runtime: node
    buildCommand: python3 -m pip install python-docx --user && npm install && cd functions && npm install
    startCommand: node functions/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
