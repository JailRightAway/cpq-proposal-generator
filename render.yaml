services:
  - type: web
    name: cpq-proposal-generator
    runtime: node
    buildCommand: npm install && cd functions && npm install
    startCommand: node functions/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
